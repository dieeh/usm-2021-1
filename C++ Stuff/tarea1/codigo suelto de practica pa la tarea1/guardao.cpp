 while (getline(file, s)) {
        if(s[0] == 'E' && stoi(s.substr(s.length() - 5,2)) < hora2) {
            for ( i = 0; i < n; i++)
            {
                if(arr[i]== s.substr(1, s.length()-8 );
            }
                
        
        }
    }